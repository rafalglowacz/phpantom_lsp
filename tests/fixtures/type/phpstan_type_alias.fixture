// test: @phpstan-type alias resolves in method return type
// feature: completion
// Adapted from phpactor type-alias/phpstan-type-alias.test
// ignore: @phpstan-type alias not resolved when used as return type in foreach
// expect: getName(
---
<?php

class User {
    public function getName(): string {}
}

/**
 * @phpstan-type UserList array<int, User>
 */
class Foo
{
    /**
     * @return UserList
     */
    public function getUsers(): array
    {
    }
}

$foo = new Foo();
foreach ($foo->getUsers() as $user) {
    $user-><>
}